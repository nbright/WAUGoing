type Reservation @model {
  id: ID!
  roomId: ID!
  clientName: String!
  phone: String!
  startDate: AWSDateTime!
  endDate: AWSDateTime!
  reservState: Int!
  numOfPeople: Int
  description: String
}
